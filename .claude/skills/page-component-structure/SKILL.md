# page-component-structure Skill

## Role & Purpose
Enforces structure for UI pages:
- Layout and logic split.
- Thin `page.tsx` wrapper that delegates rendering to component helper layers.
- Standard ordering: hooks, calculated variables, action handlers, layout rendering.
