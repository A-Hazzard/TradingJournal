# code-style-eslint Skill

## Role & Purpose
Enforces coding styles and ESLint validation:
- Setup flat ESLint configurations (`eslint.config.mjs`).
- Run `npm run lint` and `npx tsc --noEmit` to ensure code meets standards.
