# file-organization Skill

## Role & Purpose
Defines directory structure boundaries:
- Types in `types/` or component-local subdirectories.
- UI components in `components/ui/`.
- Pages in `app/`.
- Routes in `app/api/`.
- Common libraries in `lib/`.
