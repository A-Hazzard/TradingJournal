# skeleton-loaders Skill

## Role & Purpose
Enforces page loading layouts:
- All asynchronous loading paths must display a structured skeleton matching the exact components layout, rather than generic loading text/spinners.
- Skeletons must reside in `components/ui/skeletons/`.
